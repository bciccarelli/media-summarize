# Gemini API
gemini:
  api_key: "your-gemini-api-key"
  model: "gemini-2.5-flash"

# Gmail SMTP (requires app password — enable 2FA first)
# Generate app password at: https://myaccount.google.com/apppasswords
email:
  sender: "you@gmail.com"
  app_password: "xxxx xxxx xxxx xxxx"
  recipient: "you@gmail.com"

# Settings
settings:
  target_tweets: 60           # How many tweets to collect from your X feed
  target_linkedin_posts: 25   # Fewer than tweets — LinkedIn is stricter about bots
  max_digest_items: 8         # Hard cap on items in the final email
  min_signal_score: 7         # Items below this 1-10 score are filtered out
  auto_like_count: 3          # Top-N X items to like each day
  dedup_window_days: 7        # How long sent URLs stay in digest_state.json
